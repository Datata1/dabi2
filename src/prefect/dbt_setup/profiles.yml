# Inhalt für ./src/prefect/dbt_setup/profiles.yml

# Profilname (muss mit dbt_project.yml übereinstimmen)
dbt_setup:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: /app/prefect/dbt_setup/dev.duckdb
